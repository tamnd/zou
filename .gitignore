/target
/build
/dist
**/*.rs.bk
*.pending-snap
.DS_Store
/data

# cargo-mutants
/mutants.out
/mutants.out.old

# cargo-fuzz
fuzz/target/
fuzz/corpus/
fuzz/artifacts/
fuzz/coverage/
fuzz/Cargo.lock

# the node binding: the addon is built, and node_modules is node_modules
crates/zou-node/zou.node
node_modules/
package-lock.json

# the npm command line package: the bundle it downloads at install time
packaging/npm/vendor/

# the python binding: the extension is built, and so is everything
# python leaves behind while running it
crates/zou-python/python/zou/*.so
__pycache__/
*.egg-info/
